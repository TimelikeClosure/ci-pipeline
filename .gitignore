# Ignore directories auto-generated by OSes and external applications.
# Really, these should be included on a global .gitignore on each device,
# but very few people remember to create it, and all it takes is one commit to mess it up.
.DS_Store
.idea
.vscode

# Ignore device-specific app configuration
/config/**/*
!/config/**/.git*
!/config/**/*.template
!/config/index.js
.env

# Ignore external dependencies (cater to application type)
node_modules
vendor
